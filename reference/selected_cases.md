# Get selected cases

This function returns the cases of the data which are selected within a
`plotscaper` scene.

## Usage

``` r
selected_cases(x)
```

## Arguments

- x:

  A `plotscaper` scene

## Value

A numeric vector of case ids
