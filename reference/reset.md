# Reset a scene or schema

This function resets a `plotscaper` scene or schema. All selection/group
assignment will be removed, and axis limits/levels of zoom will be
restored to default.

## Usage

``` r
reset(x)
```

## Arguments

- x:

  A `plotscaper` scene or schema

## Value

The scene or schema back
